import styled from "styled-components";
import { Form, Button, Container, Jumbotron } from 'react-bootstrap';

export const CreateCategoryForm = styled(Form)``;
export const CreateCategoryButton = styled(Button)``;
export const CreateCategoryContainer = styled(Container)`
  justify-content: center;
  position: relative;
  min-height: 100vh;
`;
export const CreateCategoryJumbotron = styled(Jumbotron)`
  background: #6A1EDF;
  color: #f3f3f3;
  text-align: center;
`;
